{% extends 'base.html.twig' %}

{% block title %}New Emprunt{% endblock %}

{% block body %}
<div class="container my-5" style="background: linear-gradient(to right, #fff4e6, #ffe5b4); padding: 30px; border-radius: 12px;">
    <div class="row justify-content-center">
        <div class="col-md-8">

            <!-- Card principale avec fond marron clair -->
            <div class="card shadow-lg border-0" style="background-color: #fff1e0cc; border-radius: 12px;">
                <div class="card-header" style="background-color: #ff8c42; color: white; text-align: center; border-top-left-radius: 12px; border-top-right-radius: 12px;">
                    <h2 class="mb-0">Créer un Nouvel Emprunt 📖</h2>
                    
                </div>

                <div class="card-body">
                    <!-- Formulaire (inchangé) -->
                    {{ include('emprunt/_form.html.twig') }}
                </div>

                <!-- Footer vertical -->
                <div class="card-footer d-flex flex-column gap-2">
                    <!-- Bouton retour -->
                    <a href="{{ path('app_emprunt_index') }}" class="btn" style="background-color: #ff8c42; color: white; width: 100%;">
                        <i class="bi bi-arrow-left-circle"></i> Retour à la liste
                    </a>
                </div>
            </div>

        </div>
    </div>
</div>
{% endblock %}
